import { describe, it, expect } from 'vitest';
import { TripCardComponent } from './trip-card';

describe('TripCardComponent', () => {
  it('should create', () => {
    const component = new TripCardComponent();
    expect(component).toBeTruthy();
  });
});
